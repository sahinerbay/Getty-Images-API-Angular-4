import { SetSearchIconDirective } from './set-search-icon.directive';

describe('SetSearchIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SetSearchIconDirective();
    expect(directive).toBeTruthy();
  });
});
